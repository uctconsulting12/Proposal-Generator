# Runtime dependencies for the JD Proposal Copilot.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
pymongo>=4.7
PyJWT>=2.9
python-multipart>=0.0.9

# Proposal PDF export (pure Python, no native dependencies).
fpdf2>=2.7

# RAG stack (optional at runtime — the app degrades gracefully without it).
qdrant-client>=1.10
fastembed>=0.3
pypdf>=4.0
